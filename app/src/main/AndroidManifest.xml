<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.thomas.joseragrosshandelwolfertstetter">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/josera_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/josera_logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity"></activity>
        <activity android:name=".UserManagement.Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Artikel" />
        <activity android:name=".Bestellen" />
        <activity android:name=".pro_spezial" />
        <activity android:name=".frumi_plus" />
        <activity android:name=".UserManagement.SignUp" />
        <activity android:name=".UserManagement.ForgotPassword" />
        <activity android:name=".UserManagement.PersonalData" />
        <activity android:name=".UserManagement.Settings"></activity>
    </application>

</manifest>